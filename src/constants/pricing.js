// src/constants/pricing.js
// Global pricing rules for the current B2B taxi-passing flow.

export const MIN_RENTAL_DAYS = 30;
export const PLATFORM_DAILY_UPLIFT = 70; // ₹70 per day margin added on top of owner price
export const DEFAULT_DEPOSIT_AMOUNT = 5000; // fallback if owner doesn't specify
export const PLATFORM_DEPOSIT_UPLIFT = 2000; // flat margin added on top of owner deposit
