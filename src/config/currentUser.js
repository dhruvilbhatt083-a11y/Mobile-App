// src/config/currentUser.js
// Temporary hard-coded IDs for development. Replace once real auth is wired.

// Pretend this is the driver using the app
export const DEV_DRIVER_ID = 'user_001';

// Pretend this is the owner you’re testing with
export const DEV_OWNER_ID = 'owner_001';
